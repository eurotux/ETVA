<?php

/**
 * Base class that represents a row from the 'node' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.3.0-dev on:
 *
 * Fri Aug 28 18:22:07 2009
 *
 * @package    lib.model.om
 */
abstract class BaseEtvaNode extends BaseObject  implements Persistent {


  const PEER = 'EtvaNodePeer';

	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        EtvaNodePeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the name field.
	 * @var        string
	 */
	protected $name;

	/**
	 * The value for the memtotal field.
	 * @var        int
	 */
	protected $memtotal;

	/**
	 * The value for the memfree field.
	 * @var        int
	 */
	protected $memfree;

	/**
	 * The value for the cputotal field.
	 * @var        int
	 */
	protected $cputotal;

	/**
	 * The value for the ip field.
	 * @var        string
	 */
	protected $ip;

	/**
	 * The value for the port field.
	 * @var        int
	 */
	protected $port;

	/**
	 * The value for the uid field.
	 * @var        string
	 */
	protected $uid;

	/**
	 * The value for the network_cards field.
	 * @var        int
	 */
	protected $network_cards;

	/**
	 * The value for the state field.
	 * Note: this column has a database default value of: 1
	 * @var        int
	 */
	protected $state;

	/**
	 * The value for the created_at field.
	 * @var        string
	 */
	protected $created_at;

	/**
	 * The value for the updated_at field.
	 * @var        string
	 */
	protected $updated_at;

	/**
	 * @var        array EtvaServer[] Collection to store aggregation of EtvaServer objects.
	 */
	protected $collEtvaServers;

	/**
	 * @var        Criteria The criteria used to select the current contents of collEtvaServers.
	 */
	private $lastEtvaServerCriteria = null;

	/**
	 * @var        array EtvaPhysicalvolume[] Collection to store aggregation of EtvaPhysicalvolume objects.
	 */
	protected $collEtvaPhysicalvolumes;

	/**
	 * @var        Criteria The criteria used to select the current contents of collEtvaPhysicalvolumes.
	 */
	private $lastEtvaPhysicalvolumeCriteria = null;

	/**
	 * @var        array EtvaVolumegroup[] Collection to store aggregation of EtvaVolumegroup objects.
	 */
	protected $collEtvaVolumegroups;

	/**
	 * @var        Criteria The criteria used to select the current contents of collEtvaVolumegroups.
	 */
	private $lastEtvaVolumegroupCriteria = null;

	/**
	 * @var        array EtvaLogicalvolume[] Collection to store aggregation of EtvaLogicalvolume objects.
	 */
	protected $collEtvaLogicalvolumes;

	/**
	 * @var        Criteria The criteria used to select the current contents of collEtvaLogicalvolumes.
	 */
	private $lastEtvaLogicalvolumeCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Initializes internal state of BaseEtvaNode object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
		$this->state = 1;
	}

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [name] column value.
	 * 
	 * @return     string
	 */
	public function getName()
	{
		return $this->name;
	}

	/**
	 * Get the [memtotal] column value.
	 * 
	 * @return     int
	 */
	public function getMemtotal()
	{
		return $this->memtotal;
	}

	/**
	 * Get the [memfree] column value.
	 * 
	 * @return     int
	 */
	public function getMemfree()
	{
		return $this->memfree;
	}

	/**
	 * Get the [cputotal] column value.
	 * 
	 * @return     int
	 */
	public function getCputotal()
	{
		return $this->cputotal;
	}

	/**
	 * Get the [ip] column value.
	 * 
	 * @return     string
	 */
	public function getIp()
	{
		return $this->ip;
	}

	/**
	 * Get the [port] column value.
	 * 
	 * @return     int
	 */
	public function getPort()
	{
		return $this->port;
	}

	/**
	 * Get the [uid] column value.
	 * 
	 * @return     string
	 */
	public function getUid()
	{
		return $this->uid;
	}

	/**
	 * Get the [network_cards] column value.
	 * 
	 * @return     int
	 */
	public function getNetworkCards()
	{
		return $this->network_cards;
	}

	/**
	 * Get the [state] column value.
	 * 
	 * @return     int
	 */
	public function getState()
	{
		return $this->state;
	}

	/**
	 * Get the [optionally formatted] temporal [created_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getCreatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->created_at === null) {
			return null;
		}



		try {
			$dt = new DateTime($this->created_at);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->created_at, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [updated_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getUpdatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->updated_at === null) {
			return null;
		}



		try {
			$dt = new DateTime($this->updated_at);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->updated_at, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     EtvaNode The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = EtvaNodePeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [name] column.
	 * 
	 * @param      string $v new value
	 * @return     EtvaNode The current object (for fluent API support)
	 */
	public function setName($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->name !== $v) {
			$this->name = $v;
			$this->modifiedColumns[] = EtvaNodePeer::NAME;
		}

		return $this;
	} // setName()

	/**
	 * Set the value of [memtotal] column.
	 * 
	 * @param      int $v new value
	 * @return     EtvaNode The current object (for fluent API support)
	 */
	public function setMemtotal($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->memtotal !== $v) {
			$this->memtotal = $v;
			$this->modifiedColumns[] = EtvaNodePeer::MEMTOTAL;
		}

		return $this;
	} // setMemtotal()

	/**
	 * Set the value of [memfree] column.
	 * 
	 * @param      int $v new value
	 * @return     EtvaNode The current object (for fluent API support)
	 */
	public function setMemfree($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->memfree !== $v) {
			$this->memfree = $v;
			$this->modifiedColumns[] = EtvaNodePeer::MEMFREE;
		}

		return $this;
	} // setMemfree()

	/**
	 * Set the value of [cputotal] column.
	 * 
	 * @param      int $v new value
	 * @return     EtvaNode The current object (for fluent API support)
	 */
	public function setCputotal($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->cputotal !== $v) {
			$this->cputotal = $v;
			$this->modifiedColumns[] = EtvaNodePeer::CPUTOTAL;
		}

		return $this;
	} // setCputotal()

	/**
	 * Set the value of [ip] column.
	 * 
	 * @param      string $v new value
	 * @return     EtvaNode The current object (for fluent API support)
	 */
	public function setIp($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->ip !== $v) {
			$this->ip = $v;
			$this->modifiedColumns[] = EtvaNodePeer::IP;
		}

		return $this;
	} // setIp()

	/**
	 * Set the value of [port] column.
	 * 
	 * @param      int $v new value
	 * @return     EtvaNode The current object (for fluent API support)
	 */
	public function setPort($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->port !== $v) {
			$this->port = $v;
			$this->modifiedColumns[] = EtvaNodePeer::PORT;
		}

		return $this;
	} // setPort()

	/**
	 * Set the value of [uid] column.
	 * 
	 * @param      string $v new value
	 * @return     EtvaNode The current object (for fluent API support)
	 */
	public function setUid($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->uid !== $v) {
			$this->uid = $v;
			$this->modifiedColumns[] = EtvaNodePeer::UID;
		}

		return $this;
	} // setUid()

	/**
	 * Set the value of [network_cards] column.
	 * 
	 * @param      int $v new value
	 * @return     EtvaNode The current object (for fluent API support)
	 */
	public function setNetworkCards($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->network_cards !== $v) {
			$this->network_cards = $v;
			$this->modifiedColumns[] = EtvaNodePeer::NETWORK_CARDS;
		}

		return $this;
	} // setNetworkCards()

	/**
	 * Set the value of [state] column.
	 * 
	 * @param      int $v new value
	 * @return     EtvaNode The current object (for fluent API support)
	 */
	public function setState($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->state !== $v || $v === 1) {
			$this->state = $v;
			$this->modifiedColumns[] = EtvaNodePeer::STATE;
		}

		return $this;
	} // setState()

	/**
	 * Sets the value of [created_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     EtvaNode The current object (for fluent API support)
	 */
	public function setCreatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->created_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->created_at !== null && $tmpDt = new DateTime($this->created_at)) ? $tmpDt->format('Y-m-d\\TH:i:sO') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d\\TH:i:sO') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->created_at = ($dt ? $dt->format('Y-m-d\\TH:i:sO') : null);
				$this->modifiedColumns[] = EtvaNodePeer::CREATED_AT;
			}
		} // if either are not null

		return $this;
	} // setCreatedAt()

	/**
	 * Sets the value of [updated_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     EtvaNode The current object (for fluent API support)
	 */
	public function setUpdatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->updated_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->updated_at !== null && $tmpDt = new DateTime($this->updated_at)) ? $tmpDt->format('Y-m-d\\TH:i:sO') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d\\TH:i:sO') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->updated_at = ($dt ? $dt->format('Y-m-d\\TH:i:sO') : null);
				$this->modifiedColumns[] = EtvaNodePeer::UPDATED_AT;
			}
		} // if either are not null

		return $this;
	} // setUpdatedAt()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			// First, ensure that we don't have any columns that have been modified which aren't default columns.
			if (array_diff($this->modifiedColumns, array(EtvaNodePeer::STATE))) {
				return false;
			}

			if ($this->state !== 1) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->name = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->memtotal = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
			$this->memfree = ($row[$startcol + 3] !== null) ? (int) $row[$startcol + 3] : null;
			$this->cputotal = ($row[$startcol + 4] !== null) ? (int) $row[$startcol + 4] : null;
			$this->ip = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->port = ($row[$startcol + 6] !== null) ? (int) $row[$startcol + 6] : null;
			$this->uid = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->network_cards = ($row[$startcol + 8] !== null) ? (int) $row[$startcol + 8] : null;
			$this->state = ($row[$startcol + 9] !== null) ? (int) $row[$startcol + 9] : null;
			$this->created_at = ($row[$startcol + 10] !== null) ? (string) $row[$startcol + 10] : null;
			$this->updated_at = ($row[$startcol + 11] !== null) ? (string) $row[$startcol + 11] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 12; // 12 = EtvaNodePeer::NUM_COLUMNS - EtvaNodePeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating EtvaNode object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(EtvaNodePeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = EtvaNodePeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->collEtvaServers = null;
			$this->lastEtvaServerCriteria = null;

			$this->collEtvaPhysicalvolumes = null;
			$this->lastEtvaPhysicalvolumeCriteria = null;

			$this->collEtvaVolumegroups = null;
			$this->lastEtvaVolumegroupCriteria = null;

			$this->collEtvaLogicalvolumes = null;
			$this->lastEtvaLogicalvolumeCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseEtvaNode:delete:pre') as $callable)
    {
      $ret = call_user_func($callable, $this, $con);
      if ($ret)
      {
        return;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(EtvaNodePeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			EtvaNodePeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	

    foreach (sfMixer::getCallables('BaseEtvaNode:delete:post') as $callable)
    {
      call_user_func($callable, $this, $con);
    }

  }
	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseEtvaNode:save:pre') as $callable)
    {
      $affectedRows = call_user_func($callable, $this, $con);
      if (is_int($affectedRows))
      {
        return $affectedRows;
      }
    }


    if ($this->isNew() && !$this->isColumnModified(EtvaNodePeer::CREATED_AT))
    {
      $this->setCreatedAt(time());
    }

    if ($this->isModified() && !$this->isColumnModified(EtvaNodePeer::UPDATED_AT))
    {
      $this->setUpdatedAt(time());
    }

		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(EtvaNodePeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$affectedRows = $this->doSave($con);
			$con->commit();
    foreach (sfMixer::getCallables('BaseEtvaNode:save:post') as $callable)
    {
      call_user_func($callable, $this, $con, $affectedRows);
    }

			EtvaNodePeer::addInstanceToPool($this);
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			if ($this->isNew() ) {
				$this->modifiedColumns[] = EtvaNodePeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = EtvaNodePeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += EtvaNodePeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collEtvaServers !== null) {
				foreach ($this->collEtvaServers as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collEtvaPhysicalvolumes !== null) {
				foreach ($this->collEtvaPhysicalvolumes as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collEtvaVolumegroups !== null) {
				foreach ($this->collEtvaVolumegroups as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collEtvaLogicalvolumes !== null) {
				foreach ($this->collEtvaLogicalvolumes as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			if (($retval = EtvaNodePeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collEtvaServers !== null) {
					foreach ($this->collEtvaServers as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collEtvaPhysicalvolumes !== null) {
					foreach ($this->collEtvaPhysicalvolumes as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collEtvaVolumegroups !== null) {
					foreach ($this->collEtvaVolumegroups as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collEtvaLogicalvolumes !== null) {
					foreach ($this->collEtvaLogicalvolumes as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = EtvaNodePeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getName();
				break;
			case 2:
				return $this->getMemtotal();
				break;
			case 3:
				return $this->getMemfree();
				break;
			case 4:
				return $this->getCputotal();
				break;
			case 5:
				return $this->getIp();
				break;
			case 6:
				return $this->getPort();
				break;
			case 7:
				return $this->getUid();
				break;
			case 8:
				return $this->getNetworkCards();
				break;
			case 9:
				return $this->getState();
				break;
			case 10:
				return $this->getCreatedAt();
				break;
			case 11:
				return $this->getUpdatedAt();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = EtvaNodePeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getName(),
			$keys[2] => $this->getMemtotal(),
			$keys[3] => $this->getMemfree(),
			$keys[4] => $this->getCputotal(),
			$keys[5] => $this->getIp(),
			$keys[6] => $this->getPort(),
			$keys[7] => $this->getUid(),
			$keys[8] => $this->getNetworkCards(),
			$keys[9] => $this->getState(),
			$keys[10] => $this->getCreatedAt(),
			$keys[11] => $this->getUpdatedAt(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = EtvaNodePeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setName($value);
				break;
			case 2:
				$this->setMemtotal($value);
				break;
			case 3:
				$this->setMemfree($value);
				break;
			case 4:
				$this->setCputotal($value);
				break;
			case 5:
				$this->setIp($value);
				break;
			case 6:
				$this->setPort($value);
				break;
			case 7:
				$this->setUid($value);
				break;
			case 8:
				$this->setNetworkCards($value);
				break;
			case 9:
				$this->setState($value);
				break;
			case 10:
				$this->setCreatedAt($value);
				break;
			case 11:
				$this->setUpdatedAt($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = EtvaNodePeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setName($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setMemtotal($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setMemfree($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setCputotal($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setIp($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setPort($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setUid($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setNetworkCards($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setState($arr[$keys[9]]);
		if (array_key_exists($keys[10], $arr)) $this->setCreatedAt($arr[$keys[10]]);
		if (array_key_exists($keys[11], $arr)) $this->setUpdatedAt($arr[$keys[11]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(EtvaNodePeer::DATABASE_NAME);

		if ($this->isColumnModified(EtvaNodePeer::ID)) $criteria->add(EtvaNodePeer::ID, $this->id);
		if ($this->isColumnModified(EtvaNodePeer::NAME)) $criteria->add(EtvaNodePeer::NAME, $this->name);
		if ($this->isColumnModified(EtvaNodePeer::MEMTOTAL)) $criteria->add(EtvaNodePeer::MEMTOTAL, $this->memtotal);
		if ($this->isColumnModified(EtvaNodePeer::MEMFREE)) $criteria->add(EtvaNodePeer::MEMFREE, $this->memfree);
		if ($this->isColumnModified(EtvaNodePeer::CPUTOTAL)) $criteria->add(EtvaNodePeer::CPUTOTAL, $this->cputotal);
		if ($this->isColumnModified(EtvaNodePeer::IP)) $criteria->add(EtvaNodePeer::IP, $this->ip);
		if ($this->isColumnModified(EtvaNodePeer::PORT)) $criteria->add(EtvaNodePeer::PORT, $this->port);
		if ($this->isColumnModified(EtvaNodePeer::UID)) $criteria->add(EtvaNodePeer::UID, $this->uid);
		if ($this->isColumnModified(EtvaNodePeer::NETWORK_CARDS)) $criteria->add(EtvaNodePeer::NETWORK_CARDS, $this->network_cards);
		if ($this->isColumnModified(EtvaNodePeer::STATE)) $criteria->add(EtvaNodePeer::STATE, $this->state);
		if ($this->isColumnModified(EtvaNodePeer::CREATED_AT)) $criteria->add(EtvaNodePeer::CREATED_AT, $this->created_at);
		if ($this->isColumnModified(EtvaNodePeer::UPDATED_AT)) $criteria->add(EtvaNodePeer::UPDATED_AT, $this->updated_at);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(EtvaNodePeer::DATABASE_NAME);

		$criteria->add(EtvaNodePeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of EtvaNode (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setName($this->name);

		$copyObj->setMemtotal($this->memtotal);

		$copyObj->setMemfree($this->memfree);

		$copyObj->setCputotal($this->cputotal);

		$copyObj->setIp($this->ip);

		$copyObj->setPort($this->port);

		$copyObj->setUid($this->uid);

		$copyObj->setNetworkCards($this->network_cards);

		$copyObj->setState($this->state);

		$copyObj->setCreatedAt($this->created_at);

		$copyObj->setUpdatedAt($this->updated_at);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getEtvaServers() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addEtvaServer($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getEtvaPhysicalvolumes() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addEtvaPhysicalvolume($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getEtvaVolumegroups() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addEtvaVolumegroup($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getEtvaLogicalvolumes() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addEtvaLogicalvolume($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     EtvaNode Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     EtvaNodePeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new EtvaNodePeer();
		}
		return self::$peer;
	}

	/**
	 * Clears out the collEtvaServers collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addEtvaServers()
	 */
	public function clearEtvaServers()
	{
		$this->collEtvaServers = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collEtvaServers collection (array).
	 *
	 * By default this just sets the collEtvaServers collection to an empty array (like clearcollEtvaServers());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initEtvaServers()
	{
		$this->collEtvaServers = array();
	}

	/**
	 * Gets an array of EtvaServer objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this EtvaNode has previously been saved, it will retrieve
	 * related EtvaServers from storage. If this EtvaNode is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array EtvaServer[]
	 * @throws     PropelException
	 */
	public function getEtvaServers($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(EtvaNodePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collEtvaServers === null) {
			if ($this->isNew()) {
			   $this->collEtvaServers = array();
			} else {

				$criteria->add(EtvaServerPeer::NODE_ID, $this->id);

				EtvaServerPeer::addSelectColumns($criteria);
				$this->collEtvaServers = EtvaServerPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(EtvaServerPeer::NODE_ID, $this->id);

				EtvaServerPeer::addSelectColumns($criteria);
				if (!isset($this->lastEtvaServerCriteria) || !$this->lastEtvaServerCriteria->equals($criteria)) {
					$this->collEtvaServers = EtvaServerPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastEtvaServerCriteria = $criteria;
		return $this->collEtvaServers;
	}

	/**
	 * Returns the number of related EtvaServer objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related EtvaServer objects.
	 * @throws     PropelException
	 */
	public function countEtvaServers(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(EtvaNodePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collEtvaServers === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(EtvaServerPeer::NODE_ID, $this->id);

				$count = EtvaServerPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(EtvaServerPeer::NODE_ID, $this->id);

				if (!isset($this->lastEtvaServerCriteria) || !$this->lastEtvaServerCriteria->equals($criteria)) {
					$count = EtvaServerPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collEtvaServers);
				}
			} else {
				$count = count($this->collEtvaServers);
			}
		}
		$this->lastEtvaServerCriteria = $criteria;
		return $count;
	}

	/**
	 * Method called to associate a EtvaServer object to this object
	 * through the EtvaServer foreign key attribute.
	 *
	 * @param      EtvaServer $l EtvaServer
	 * @return     void
	 * @throws     PropelException
	 */
	public function addEtvaServer(EtvaServer $l)
	{
		if ($this->collEtvaServers === null) {
			$this->initEtvaServers();
		}
		if (!in_array($l, $this->collEtvaServers, true)) { // only add it if the **same** object is not already associated
			array_push($this->collEtvaServers, $l);
			$l->setEtvaNode($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this EtvaNode is new, it will return
	 * an empty collection; or if this EtvaNode has previously
	 * been saved, it will retrieve related EtvaServers from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in EtvaNode.
	 */
	public function getEtvaServersJoinEtvaLogicalvolume($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(EtvaNodePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collEtvaServers === null) {
			if ($this->isNew()) {
				$this->collEtvaServers = array();
			} else {

				$criteria->add(EtvaServerPeer::NODE_ID, $this->id);

				$this->collEtvaServers = EtvaServerPeer::doSelectJoinEtvaLogicalvolume($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(EtvaServerPeer::NODE_ID, $this->id);

			if (!isset($this->lastEtvaServerCriteria) || !$this->lastEtvaServerCriteria->equals($criteria)) {
				$this->collEtvaServers = EtvaServerPeer::doSelectJoinEtvaLogicalvolume($criteria, $con, $join_behavior);
			}
		}
		$this->lastEtvaServerCriteria = $criteria;

		return $this->collEtvaServers;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this EtvaNode is new, it will return
	 * an empty collection; or if this EtvaNode has previously
	 * been saved, it will retrieve related EtvaServers from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in EtvaNode.
	 */
	public function getEtvaServersJoinsfGuardGroup($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(EtvaNodePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collEtvaServers === null) {
			if ($this->isNew()) {
				$this->collEtvaServers = array();
			} else {

				$criteria->add(EtvaServerPeer::NODE_ID, $this->id);

				$this->collEtvaServers = EtvaServerPeer::doSelectJoinsfGuardGroup($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(EtvaServerPeer::NODE_ID, $this->id);

			if (!isset($this->lastEtvaServerCriteria) || !$this->lastEtvaServerCriteria->equals($criteria)) {
				$this->collEtvaServers = EtvaServerPeer::doSelectJoinsfGuardGroup($criteria, $con, $join_behavior);
			}
		}
		$this->lastEtvaServerCriteria = $criteria;

		return $this->collEtvaServers;
	}

	/**
	 * Clears out the collEtvaPhysicalvolumes collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addEtvaPhysicalvolumes()
	 */
	public function clearEtvaPhysicalvolumes()
	{
		$this->collEtvaPhysicalvolumes = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collEtvaPhysicalvolumes collection (array).
	 *
	 * By default this just sets the collEtvaPhysicalvolumes collection to an empty array (like clearcollEtvaPhysicalvolumes());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initEtvaPhysicalvolumes()
	{
		$this->collEtvaPhysicalvolumes = array();
	}

	/**
	 * Gets an array of EtvaPhysicalvolume objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this EtvaNode has previously been saved, it will retrieve
	 * related EtvaPhysicalvolumes from storage. If this EtvaNode is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array EtvaPhysicalvolume[]
	 * @throws     PropelException
	 */
	public function getEtvaPhysicalvolumes($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(EtvaNodePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collEtvaPhysicalvolumes === null) {
			if ($this->isNew()) {
			   $this->collEtvaPhysicalvolumes = array();
			} else {

				$criteria->add(EtvaPhysicalvolumePeer::NODE_ID, $this->id);

				EtvaPhysicalvolumePeer::addSelectColumns($criteria);
				$this->collEtvaPhysicalvolumes = EtvaPhysicalvolumePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(EtvaPhysicalvolumePeer::NODE_ID, $this->id);

				EtvaPhysicalvolumePeer::addSelectColumns($criteria);
				if (!isset($this->lastEtvaPhysicalvolumeCriteria) || !$this->lastEtvaPhysicalvolumeCriteria->equals($criteria)) {
					$this->collEtvaPhysicalvolumes = EtvaPhysicalvolumePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastEtvaPhysicalvolumeCriteria = $criteria;
		return $this->collEtvaPhysicalvolumes;
	}

	/**
	 * Returns the number of related EtvaPhysicalvolume objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related EtvaPhysicalvolume objects.
	 * @throws     PropelException
	 */
	public function countEtvaPhysicalvolumes(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(EtvaNodePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collEtvaPhysicalvolumes === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(EtvaPhysicalvolumePeer::NODE_ID, $this->id);

				$count = EtvaPhysicalvolumePeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(EtvaPhysicalvolumePeer::NODE_ID, $this->id);

				if (!isset($this->lastEtvaPhysicalvolumeCriteria) || !$this->lastEtvaPhysicalvolumeCriteria->equals($criteria)) {
					$count = EtvaPhysicalvolumePeer::doCount($criteria, $con);
				} else {
					$count = count($this->collEtvaPhysicalvolumes);
				}
			} else {
				$count = count($this->collEtvaPhysicalvolumes);
			}
		}
		$this->lastEtvaPhysicalvolumeCriteria = $criteria;
		return $count;
	}

	/**
	 * Method called to associate a EtvaPhysicalvolume object to this object
	 * through the EtvaPhysicalvolume foreign key attribute.
	 *
	 * @param      EtvaPhysicalvolume $l EtvaPhysicalvolume
	 * @return     void
	 * @throws     PropelException
	 */
	public function addEtvaPhysicalvolume(EtvaPhysicalvolume $l)
	{
		if ($this->collEtvaPhysicalvolumes === null) {
			$this->initEtvaPhysicalvolumes();
		}
		if (!in_array($l, $this->collEtvaPhysicalvolumes, true)) { // only add it if the **same** object is not already associated
			array_push($this->collEtvaPhysicalvolumes, $l);
			$l->setEtvaNode($this);
		}
	}

	/**
	 * Clears out the collEtvaVolumegroups collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addEtvaVolumegroups()
	 */
	public function clearEtvaVolumegroups()
	{
		$this->collEtvaVolumegroups = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collEtvaVolumegroups collection (array).
	 *
	 * By default this just sets the collEtvaVolumegroups collection to an empty array (like clearcollEtvaVolumegroups());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initEtvaVolumegroups()
	{
		$this->collEtvaVolumegroups = array();
	}

	/**
	 * Gets an array of EtvaVolumegroup objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this EtvaNode has previously been saved, it will retrieve
	 * related EtvaVolumegroups from storage. If this EtvaNode is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array EtvaVolumegroup[]
	 * @throws     PropelException
	 */
	public function getEtvaVolumegroups($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(EtvaNodePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collEtvaVolumegroups === null) {
			if ($this->isNew()) {
			   $this->collEtvaVolumegroups = array();
			} else {

				$criteria->add(EtvaVolumegroupPeer::NODE_ID, $this->id);

				EtvaVolumegroupPeer::addSelectColumns($criteria);
				$this->collEtvaVolumegroups = EtvaVolumegroupPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(EtvaVolumegroupPeer::NODE_ID, $this->id);

				EtvaVolumegroupPeer::addSelectColumns($criteria);
				if (!isset($this->lastEtvaVolumegroupCriteria) || !$this->lastEtvaVolumegroupCriteria->equals($criteria)) {
					$this->collEtvaVolumegroups = EtvaVolumegroupPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastEtvaVolumegroupCriteria = $criteria;
		return $this->collEtvaVolumegroups;
	}

	/**
	 * Returns the number of related EtvaVolumegroup objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related EtvaVolumegroup objects.
	 * @throws     PropelException
	 */
	public function countEtvaVolumegroups(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(EtvaNodePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collEtvaVolumegroups === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(EtvaVolumegroupPeer::NODE_ID, $this->id);

				$count = EtvaVolumegroupPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(EtvaVolumegroupPeer::NODE_ID, $this->id);

				if (!isset($this->lastEtvaVolumegroupCriteria) || !$this->lastEtvaVolumegroupCriteria->equals($criteria)) {
					$count = EtvaVolumegroupPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collEtvaVolumegroups);
				}
			} else {
				$count = count($this->collEtvaVolumegroups);
			}
		}
		$this->lastEtvaVolumegroupCriteria = $criteria;
		return $count;
	}

	/**
	 * Method called to associate a EtvaVolumegroup object to this object
	 * through the EtvaVolumegroup foreign key attribute.
	 *
	 * @param      EtvaVolumegroup $l EtvaVolumegroup
	 * @return     void
	 * @throws     PropelException
	 */
	public function addEtvaVolumegroup(EtvaVolumegroup $l)
	{
		if ($this->collEtvaVolumegroups === null) {
			$this->initEtvaVolumegroups();
		}
		if (!in_array($l, $this->collEtvaVolumegroups, true)) { // only add it if the **same** object is not already associated
			array_push($this->collEtvaVolumegroups, $l);
			$l->setEtvaNode($this);
		}
	}

	/**
	 * Clears out the collEtvaLogicalvolumes collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addEtvaLogicalvolumes()
	 */
	public function clearEtvaLogicalvolumes()
	{
		$this->collEtvaLogicalvolumes = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collEtvaLogicalvolumes collection (array).
	 *
	 * By default this just sets the collEtvaLogicalvolumes collection to an empty array (like clearcollEtvaLogicalvolumes());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initEtvaLogicalvolumes()
	{
		$this->collEtvaLogicalvolumes = array();
	}

	/**
	 * Gets an array of EtvaLogicalvolume objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this EtvaNode has previously been saved, it will retrieve
	 * related EtvaLogicalvolumes from storage. If this EtvaNode is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array EtvaLogicalvolume[]
	 * @throws     PropelException
	 */
	public function getEtvaLogicalvolumes($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(EtvaNodePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collEtvaLogicalvolumes === null) {
			if ($this->isNew()) {
			   $this->collEtvaLogicalvolumes = array();
			} else {

				$criteria->add(EtvaLogicalvolumePeer::NODE_ID, $this->id);

				EtvaLogicalvolumePeer::addSelectColumns($criteria);
				$this->collEtvaLogicalvolumes = EtvaLogicalvolumePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(EtvaLogicalvolumePeer::NODE_ID, $this->id);

				EtvaLogicalvolumePeer::addSelectColumns($criteria);
				if (!isset($this->lastEtvaLogicalvolumeCriteria) || !$this->lastEtvaLogicalvolumeCriteria->equals($criteria)) {
					$this->collEtvaLogicalvolumes = EtvaLogicalvolumePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastEtvaLogicalvolumeCriteria = $criteria;
		return $this->collEtvaLogicalvolumes;
	}

	/**
	 * Returns the number of related EtvaLogicalvolume objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related EtvaLogicalvolume objects.
	 * @throws     PropelException
	 */
	public function countEtvaLogicalvolumes(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(EtvaNodePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collEtvaLogicalvolumes === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(EtvaLogicalvolumePeer::NODE_ID, $this->id);

				$count = EtvaLogicalvolumePeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(EtvaLogicalvolumePeer::NODE_ID, $this->id);

				if (!isset($this->lastEtvaLogicalvolumeCriteria) || !$this->lastEtvaLogicalvolumeCriteria->equals($criteria)) {
					$count = EtvaLogicalvolumePeer::doCount($criteria, $con);
				} else {
					$count = count($this->collEtvaLogicalvolumes);
				}
			} else {
				$count = count($this->collEtvaLogicalvolumes);
			}
		}
		$this->lastEtvaLogicalvolumeCriteria = $criteria;
		return $count;
	}

	/**
	 * Method called to associate a EtvaLogicalvolume object to this object
	 * through the EtvaLogicalvolume foreign key attribute.
	 *
	 * @param      EtvaLogicalvolume $l EtvaLogicalvolume
	 * @return     void
	 * @throws     PropelException
	 */
	public function addEtvaLogicalvolume(EtvaLogicalvolume $l)
	{
		if ($this->collEtvaLogicalvolumes === null) {
			$this->initEtvaLogicalvolumes();
		}
		if (!in_array($l, $this->collEtvaLogicalvolumes, true)) { // only add it if the **same** object is not already associated
			array_push($this->collEtvaLogicalvolumes, $l);
			$l->setEtvaNode($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this EtvaNode is new, it will return
	 * an empty collection; or if this EtvaNode has previously
	 * been saved, it will retrieve related EtvaLogicalvolumes from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in EtvaNode.
	 */
	public function getEtvaLogicalvolumesJoinEtvaVolumegroup($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(EtvaNodePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collEtvaLogicalvolumes === null) {
			if ($this->isNew()) {
				$this->collEtvaLogicalvolumes = array();
			} else {

				$criteria->add(EtvaLogicalvolumePeer::NODE_ID, $this->id);

				$this->collEtvaLogicalvolumes = EtvaLogicalvolumePeer::doSelectJoinEtvaVolumegroup($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(EtvaLogicalvolumePeer::NODE_ID, $this->id);

			if (!isset($this->lastEtvaLogicalvolumeCriteria) || !$this->lastEtvaLogicalvolumeCriteria->equals($criteria)) {
				$this->collEtvaLogicalvolumes = EtvaLogicalvolumePeer::doSelectJoinEtvaVolumegroup($criteria, $con, $join_behavior);
			}
		}
		$this->lastEtvaLogicalvolumeCriteria = $criteria;

		return $this->collEtvaLogicalvolumes;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collEtvaServers) {
				foreach ((array) $this->collEtvaServers as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collEtvaPhysicalvolumes) {
				foreach ((array) $this->collEtvaPhysicalvolumes as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collEtvaVolumegroups) {
				foreach ((array) $this->collEtvaVolumegroups as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collEtvaLogicalvolumes) {
				foreach ((array) $this->collEtvaLogicalvolumes as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collEtvaServers = null;
		$this->collEtvaPhysicalvolumes = null;
		$this->collEtvaVolumegroups = null;
		$this->collEtvaLogicalvolumes = null;
	}


  public function __call($method, $arguments)
  {
    if (!$callable = sfMixer::getCallable('BaseEtvaNode:'.$method))
    {
      throw new sfException(sprintf('Call to undefined method BaseEtvaNode::%s', $method));
    }

    array_unshift($arguments, $this);

    return call_user_func_array($callable, $arguments);
  }


} // BaseEtvaNode
